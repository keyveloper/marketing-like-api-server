spring:
  application:
    name: marketing-like-api-server
  profiles:
    group:
      local: db-local
      prod: db-prod
      aws: db-aws
  config:
    import: "classpath:application-db.yaml"

server:
  port: 8083